# No third-party dependencies. The Lambda handler uses only Python's
# standard library (json, logging, random, time), so nothing needs to be
# installed or packaged beyond backend/lambda_function.py.
